<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zimmermann.larissa.legislativoapp"
    android:versionCode="200"
    android:versionName="2.0.0">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PropDetailsActivity"
            android:label="@string/title_activity_prop_details2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DevelopersActivity"
            android:label="@string/developers_title"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".InformationActivity"
            android:label="@string/saiba_mais"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>